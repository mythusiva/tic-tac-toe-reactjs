{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd ../client; npm i; npm run build; cp -vr build ../server/dist; cd ../server",
    "start": "node --env-file=.env server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mythu Siva",
  "license": "ISC",
  "dependencies": {
    "express": "4.18.2",
    "fflag-ms": "~0.0.16",
    "path": "0.12.7"
  }
}
